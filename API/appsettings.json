{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "TokenKey": "Secret key that only the server knows, Secret key that only the server knows, Secret key that only the server knows, Secret key that only the server knows, Secret key that only the server knows, Secret key that only the server knows, Secret key that only the server knows, Secret key that only the server knows, ",
  "AllowedHosts": "*"
}
